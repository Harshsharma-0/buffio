#include "buffio/instance.hpp"

buffio::instance::instance() {}
int buffio::instance::init() {
  return 0;
}
int buffio::instance::start() { return 0; }
//int buffio::instance::push(buffio::task task) { return 0; }

