#ifndef BUFFIO_OS
#define BUFFIO_OS


#include <cstdint>
#include <cstddef>
#include <errno.h>
#include <sys/types.h>
#include <sys/uio.h>
#include <sys/stat.h>
#include <cstring>
#include <fcntl.h>
#include <cassert>

#define BUFFIO_OS_INSERT(lin,bsd,wind) lin
#define BUFFIO_LIN_INSERT(codeSection) codeSection
#define BUFFIO_WIN_INSERT(codeSection)

#define BUFFIO_WORKER_QUEUE_ORDER @BUFFIO_WORKER_QUEUE_ORDER@
#define BUFFIO_FILE_VEC_MAX @BUFFIO_FILE_VEC_MAX@

#define BUFFIO_SLEEP_QUEUE_ORDER 4
#define BUFFIO_MAX_WORKER 16


#cmakedefine BUFFIO_IO_URING_ONLY
#cmakedefine BUFFIO_BACKEND_IO_URING
#cmakedefine BUFFIO_OS_LINUX
#cmakedefine BUFFIO_OS_WINDOWS
#cmakedefine BUFFIO_OS_BSD


using buffio_fd = int;
constexpr int BUFFIO_FD_INVALID = -1;

#endif
