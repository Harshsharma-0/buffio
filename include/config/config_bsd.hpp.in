#ifndef BUFFIO_OS
#define BUFFIO_OS



#define BUFFIO_WORKER_QUEUE_ORDER @BUFFIO_WORKER_QUEUE_ORDER@
#define BUFFIO_FILE_VEC_MAX @BUFFIO_FILE_VEC_MAX@

#define BUFFIO_SLEEP_QUEUE_ORDER 4
#define BUFFIO_MAX_WORKER 16

#cmakedefine BUFFIO_WORKER_DEFAULT
#cmakedefine BUFFIO_WORKER_IO_URING
#cmakedefine BUFFIO_WORKER_IOCP

using BFLAG = int;
using BMODE = uint32_t;
using BFDDEF = BUFFIO_OS_INSERT(int,int,uintptr_t);
constexpr int BFD_INVALID = -1;

#endif
